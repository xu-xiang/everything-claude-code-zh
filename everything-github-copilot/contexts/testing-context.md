# 测试上下文

## 目标

让 Copilot 生成的测试更实用，而不是只追求覆盖率数字。

## 原则

1. 优先覆盖关键业务路径。
2. 覆盖正常路径、异常路径、边界条件。
3. Mock 只用于必要依赖。
4. 不把实现细节绑定得过死。
5. 如果难以写单元测试，应指出耦合原因并建议重构点。

## Android 场景

- ViewModel：状态变化、错误态、加载态
- Repository：数据来源切换、异常处理
- UseCase：输入输出逻辑
- Service：边界行为、错误处理